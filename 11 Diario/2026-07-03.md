---
tags: [diario]
date: 2026-07-03
---

# 2026-07-03

- Definida la arquitectura del proyecto ([[Arquitectura del proyecto]]) y migrada la estructura de carpetas del esquema numerado al definitivo.
- Creado `IA-LOCAL.code-workspace` (varias vueltas por rutas relativas mal calculadas: recordar que son relativas a la ubicacion del propio archivo `.code-workspace`, no a la raiz del proyecto).
- Bootstrap v0.1 (Doctor) y v0.2 (menu principal) construidos.
- Docker + n8n levantados por primera vez ([[Docker Compose - n8n]]).
- Bootstrap v0.3 (Docker Manager) construido y probado.
- Empezado v0.4 (N8n Manager): exportacion de workflows probada con exito tras crear un workflow de prueba en n8n.

## Problemas resueltos hoy

- Scripts `.ps1` bloqueados por Windows (`Unauthorized Access`) -> `Set-ExecutionPolicy RemoteSigned` + `Unblock-File`.
- Error de parseo en un script por un guion largo (caracter no-ASCII) -> desde entonces, todos los `.ps1` se escriben en ASCII puro.
